import { TYPES as AuthTypes } from '../modules/auth/config/types';
import { TYPES as TranscodingTypes } from '../modules/transcoding/config/types';

export const TYPES = {
  ...AuthTypes,
  ...TranscodingTypes,
};
